<!DOCTYPE html>
<html>
<head>
  <title>Submit Load</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    label { font-weight: bold; }
    select, input { margin-bottom: 15px; width: 200px; }
    .summary { background: #f0f0f0; padding: 10px; margin-bottom: 20px; }
  </style>
</head>
<body>
  <h1>🚜 Submit Load</h1>

  <!-- ✅ Summary Section -->
  <div class="summary">
    <p><strong>Total Gallons Today:</strong> <%= typeof totalGallons !== 'undefined' ? totalGallons : 0 %></p>
    <% if (typeof lastLoad !== 'undefined' && lastLoad) { %>
      <p><strong>Last Load:</strong> <%= new Date(lastLoad.timestamp).toLocaleString() %> by <%= lastLoad.tractor?.name || "Unknown" %></p>
    <% } else { %>
      <p><em>No loads submitted yet.</em></p>
    <% } %>
  </div>

  <!-- ✅ Load Submission Form -->
  <form action="/load" method="POST">
    <label for="tractor">Tractor:</label><br>
    <select name="tractor" id="tractorSelect" required>
      <% tractors.forEach(t => { %>
        <option value="<%= t._id %>"><%= t.name %></option>
      <% }) %>
    </select><br>

    <label for="gallons">Gallons:</label><br>
    <input type="number" name="gallons" id="gallonsInput" required><br>

    <label for="farm">Farm:</label><br>
    <select name="farm" required>
      <% farms.forEach(f => { %>
        <option value="<%= f._id %>"><%= f.name %></option>
      <% }) %>
    </select><br>

    <label for="field">Field:</label><br>
    <select name="field" required>
      <% fields.forEach(fl => { %>
        <option value="<%= fl._id %>"><%= fl.name %></option>
      <% }) %>
    </select><br>

    <label for="pit">Pit:</label><br>
    <select name="pit" required>
      <% pits.forEach(p => { %>
        <option value="<%= p._id %>"><%= p.name %></
